# Pin these requirements to the latest versions when you create a new project,
# e.g. Django==3.0.2.
Django
psycopg2
gunicorn
django-environ
whitenoise
django-webpack-loader
sentry-sdk

# Testing requirements
pytest
pytest-django
pytest-mock
flake8

# Project setup requirements
pip-chill
